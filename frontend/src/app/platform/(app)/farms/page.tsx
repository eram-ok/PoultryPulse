import {
  PlatformFarmRegistry,
} from "@/components/platform/platform-farm-registry"

export const metadata = {
  title: "Farm registry",
}

export default function PlatformFarmsPage() {
  return <PlatformFarmRegistry />
}
